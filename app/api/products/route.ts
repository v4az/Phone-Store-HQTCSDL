// API Route: /api/products
// GET  — return product list
// POST — create a new product + optional variant

import { NextResponse } from "next/server";

export async function GET() {
  // TODO: implement
  return NextResponse.json([]);
}
